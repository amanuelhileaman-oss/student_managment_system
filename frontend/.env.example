# Frontend Environment Configuration
# In development, leave empty to leverage the local Vite proxy (/api)
# In production, specify your hosted backend API URL (e.g. on Render, Railway, or AWS)
VITE_API_URL=http://localhost:5000/api
